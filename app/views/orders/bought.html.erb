<%=image_tag('3.png', class:"hero-body")%></br>

<h1> 👋 Hello <%= current_user.username%>!</h1>

<h3>Here are the meals that you have claimed 🌞 :</h3>

<div class="card">
  <body>
    <% @bought_items.each do |item| %>
      <span>
        <td><%= item.listing.title %></td>
        <td> Cusine:<%= item.listing.cuisine %></td>
        <td><%= item.listing.description %></td>
        <td> Allergies: <%= item.listing.allergy.name %></td>
        <td> Date Claimed:<%= item.listing.available_date %></td>
        <td> Price: $<%= item.listing.price %></td>
        <td> Bought from:<%= item.listing.seller.username %></td>
        <h5> Location picked up: 
            <%= listing.listing.seller.address.street_number %>,
            <%= listing.listing.seller.address.street_name %>,
            <%= listing.listing.seller.address.suburb %>,
            <%= listing.listing.seller.address.postcode %></h5>
      </span>
    <% end %>
  </body>
</div>

